{
  "mcpServers": {
    "ambiguous": {
      "type": "http",
      "url": "https://app.ambiguous.ai/mcp",
      "headers": {
        "Authorization": "Bearer ${AMBI_AGENT_KEY:-${user_config.apiToken}}"
      }
    }
  }
}
