{
  "mcpServers": {
    "ambiguous": {
      "type": "http",
      "url": "https://app.ambiguous.ai/mcp",
      "headers": {
        "Authorization": "Bearer ${AMBI_API_TOKEN:-${user_config.apiToken}}"
      }
    }
  }
}
