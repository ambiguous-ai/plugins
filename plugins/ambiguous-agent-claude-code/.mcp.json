{
  "mcpServers": {
    "ambiguous": {
      "type": "http",
      "url": "${user_config.origin}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.apiToken}"
      }
    }
  }
}
