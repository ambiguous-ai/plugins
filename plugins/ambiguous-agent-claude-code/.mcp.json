{
  "mcpServers": {
    "ambiguous": {
      "type": "http",
      "url": "${user_config.origin}/mcp",
      "headers": {
        "Authorization": "Bearer ${AMBI_AGENT_KEY:-${user_config.apiToken}}"
      }
    }
  }
}
